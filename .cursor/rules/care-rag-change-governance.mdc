---
description: Care RAG API 變更分級（T0–T3）、T1 預授權、ECR、與 SSOT／批次自動化並存
alwaysApply: true
---

# Care RAG API — 變更治理（Cursor Project Rule）

## 與既有原則並存

- **SSOT**：`hi_level_design.md`、`detailed_design_claude.md` 等設計文件仍為實作依據；與文件衝突時 **不得** 以本規則覆寫，應走 **T3 / ECR**。
- **測試隔離**：測試不得污染 production；mock/fake 須完整對齊介面簽章。
- **歷史註解**：檔案頂部更新歷史須保留並依專案格式新增紀錄。

## 變更分級（Tier）

| 等級 | 範圍 | AI 行為 |
|------|------|---------|
| **T0** | 註解、錯字、純格式化、不影響行為之語法修正 | 可直接修改；仍須保留歷史註解慣例。 |
| **T1** | **預授權缺陷修復**（見下節「T1 預授權」） | 可實作；**必須**附測試或明確驗證步驟；**禁止**藉機大規模重構。若與 SSOT 牴觸 → **升級 T3**。 |
| **T2** | 新行為、或設計**已涵蓋**之行為調整 | 先提出 **至少兩方案**（其一為 **維持現狀**）→ 取得使用者 **一句話批准** 後再改程式。 |
| **T3** | 設計矛盾、未定義、對外 API 契約、安全／法遵 | **Stop → Analyze → Propose（≥2 方案）→ Wait**；未批准前 **不修改** 相關程式。 |

## T1 預授權（RAG／Graph／檢索相關缺陷）

下列視為 **缺陷修復**，得依 T1 直接實作（仍須測試／Postman 或腳本驗證說明）：

1. **假陽性**：查詢關鍵字與圖譜 **`type`／schema 類名** 等欄位誤匹配（例：英文 `Organization` 命中 `type = Organization`）。
2. **誤導性分數**：非語意相似度卻當作信心度（例：**固定常數**分數）；應區分來源、降權或改欄位語意。
3. **無有效來源仍硬答**：無合格 embedding／無合理 graph 命中時，改為符合 API 契約之 **「未涵蓋／無對應資料」** 等明確表述（若 schema 允許）。

**不屬於 T1**：新端點、檢索流程重寫、換模型、改契約 → **T2 或 T3**。

## 批次自動化 vs 對話內任務

- **無人值守批次腳本／大範圍自動修改**：維持「僅註解／語法」除非使用者 **另行紅線授權**。
- **對話中使用者明確指派之任務**：適用本分級（T0–T3）；**邊界有疑義時採較嚴一級**。

## SSOT 缺口時

若設計文件 **未載明** 檢索 fallback、門檻、錯誤訊息等行為：

1. 先於 `docs/adr/` 或 `dev_readme.md` 增補 **「預設行為」** 簡短說明（含日期與摘要）。
2. 再依 **T1 或 T2** 實作；並註明是否後續併入主設計文件。

## 摘要檔

完整結論、優缺點對照與還原說明：`.cursor/RULER_SUMMARY.md`。
